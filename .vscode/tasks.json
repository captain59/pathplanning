{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Path Planning",
            "type": "shell",
            "command": "g++ -std=c++14 *.cpp `pkg-config --cflags --libs opencv`",
            "args": [], 
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}